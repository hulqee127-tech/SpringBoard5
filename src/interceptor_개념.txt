필터    ->  AOP
Filter  

web 서버에 설정해서 실행 : web.xml 등록 <- tomcat
모든페이지에서 실행되어야 하는 코드가 존재
필터에 해당소스를 등록
모든 소스는 필터를 거쳐서 실행하도록 설정

모든 페이지에서 로그인체크하려면
 Interceptor 객체를 이용하여 
   Interceptor 내에 로그인을 체크하는 기능을 넣어서 
    모든 페이지에서 실행되도록 한다
